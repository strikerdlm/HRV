# Mission Control - Flight Surgeon - Python Dependencies
# Last updated: 2025-12-18
# Version: 1.8.27

# ===========================================================================
# CORE DEPENDENCIES (Required)
# ===========================================================================
# STREAMLIT VERSION HISTORY:
# - 1.35.0: Tabs don't load properly - DO NOT USE
# - 1.36.0: Last stable release BEFORE fragment changes (RECOMMENDED)
# - 1.37.0+: Introduced @st.fragment causing SessionInfo/setIn race errors
# - 1.40.2: Has cosmetic error popups but functionally works
# Using 1.36.0 as the most stable version for this complex app.
streamlit==1.36.0
requests>=2.31,<3
pandas>=2.2,<2.3
numpy>=1.26,<2.2
scipy>=1.11,<2.0
python-dotenv>=1.0,<2.0
beautifulsoup4>=4.12,<5
openai>=1.55,<2

# ===========================================================================
# SCIENTIFIC COMPUTING
# ===========================================================================
# Signal processing and HRV analysis
scikit-learn>=1.3,<1.6  # ML algorithms (ElasticNet, RF, GB, etc.)
statsmodels>=0.14,<0.15  # Mixed-effects models for longitudinal cohort inference

# ===========================================================================
# PERFORMANCE OPTIMIZATION (Recommended)
# ===========================================================================
# These packages significantly improve performance on CPU-only systems
numba>=0.59,<1  # JIT compilation for 2-5x speedup on advanced HRV metrics
psutil>=5.9,<6  # CPU detection and process priority management

# ===========================================================================
# DATA VISUALIZATION
# ===========================================================================
# Interactive charts (ECharts is embedded via JS; Plotly used as export fallback)
plotly>=5.0,<6
# Static plot exports (PNG/SVG/PDF) for Plotly
kaleido>=0.2,<1

# ===========================================================================
# DEVICE INTEGRATIONS (Optional)
# ===========================================================================

# ActiGraph GT3X support
pygt3x>=0.4  # Uncomment for ActiGraph GT3X file support

# Somfit Pro EDF support
pyedflib>=0.1.30  # Uncomment for EDF file support (Somfit, PSG)

# Garmin Connect (requires auth)
fitparse>=1.2,<2.0  # Garmin FIT parsing for Vivosmart 5 and wellness FIT files
garminconnect>=0.2.36

# ===========================================================================
# GPU ACCELERATION (Optional - NVIDIA CUDA)
# ===========================================================================
# The app works perfectly without GPU - CPU fallback is automatic.
# Install manually if you have an NVIDIA GPU for 2-10x speedup:
#
# For RTX 50xx/40xx/30xx (CUDA 12.x):
#   pip install cupy-cuda12x
#
# For RTX 20xx/10xx (CUDA 11.x):
#   pip install cupy-cuda11x
#
# Verify installation:
#   python -c "import cupy; print(cupy.cuda.runtime.getDeviceProperties(0)['name'])"
#
# cupy-cuda12x>=13.0  # Uncomment for CUDA 12.x GPUs

# ===========================================================================
# DATABASE & PERSISTENCE
# ===========================================================================
# SQLite is built into Python - no extra dependencies needed
# For PostgreSQL deployment:
# psycopg2-binary>=2.9,<3  # Uncomment for PostgreSQL support

# ===========================================================================
# DEVELOPMENT & TESTING (Recommended)
# ===========================================================================
pytest>=7.4,<10
hypothesis>=6.90,<7
mypy>=1.8,<2
ruff>=0.3,<1
bandit>=1.7,<2
